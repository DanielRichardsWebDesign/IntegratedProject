@model IEnumerable<HealthApp.Models.Condition>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Condition1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ConditionNotes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Patient.PatientFName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Condition1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ConditionNotes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Patient.PatientFName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ConditionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ConditionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ConditionID })
        </td>
    </tr>
}

</table>
